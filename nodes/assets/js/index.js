/*
alert(document.body.innerHTML); //only in the body
alert(document.documentElement.innerHTML); // all elements from the page*/
document.getElementById("p02").innerHTML = document.getElementById("h101").firstChild.nodeValue;